name: models
description: Package storing the app-wide models for API and database
version: 1.0.0+1
publish_to: none

environment:
  sdk: ^3.8.0

dependencies:
  clock: ^1.1.2
  equatable: ^2.0.7
  flutter:
    sdk: flutter
  freezed_annotation: ^3.1.0
  hive: ^2.2.3
  hive_flutter:
  intl: ^0.20.2
  json_annotation: ^4.9.0

dev_dependencies:
  build_runner: ^2.5.4
  freezed: ^3.2.0
  json_serializable: ^6.6.2
  very_good_analysis: ^9.0.0