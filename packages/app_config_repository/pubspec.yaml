name: app_config_repository
description: Dart package which manages the app configuration domain.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.8.1

dependencies:
  cloud_firestore: ^6.0.0
  equatable: ^2.0.7
  flutter:
    sdk: flutter
  json_annotation: ^4.9.0
  models:
    path: ../models
  remote_config_client:
    path: ../remote_config_client/remote_config_client

dev_dependencies:
  build_runner: ^2.3.3
  flutter_test:
    sdk: flutter
  json_serializable: ^6.10.0
  mocktail: ^1.0.4
  very_good_analysis: ^9.0.0
