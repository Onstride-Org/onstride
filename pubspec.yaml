name: gl_horses
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.8+49

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  flutter_web_plugins:
    sdk: flutter

  # Flutter packages
  package_info_plus: ^9.0.0
  appsflyer_sdk: ^6.17.7+1
  cloud_firestore: ^6.0.0
  cupertino_icons: ^1.0.8
  firebase_core: ^4.0.0
  flutter_displaymode: ^0.6.0
  flutter_dotenv: ^5.2.1
  flutter_screenutil: ^5.9.3
  go_router: ^16.0.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  intl: ^0.20.2
  url_launcher: ^6.3.2
  path_provider: ^2.1.5
  table_calendar: ^3.2.0
  freezed_annotation: ^3.1.0
  riverpod_annotation: ^2.6.1
  mask_text_input_formatter: ^2.9.0
  keyboard_dismisser: ^3.0.0
  # Local packages
  account_repository:
    path: packages/account_repository
  app_config_repository:
    path: packages/app_config_repository
  app_ui:
    path: packages/app_ui
  auth_repository:
    path: packages/auth_repository
  firebase_remote_config_client:
    path: packages/remote_config_client/firebase_remote_config_client
  remote_config_client:
    path: packages/remote_config_client/remote_config_client
  authentication_client:
    path: packages/authentication_client/authentication_client
  database_client:
    path: packages/database_client
  data_provider_client:
    path: packages/data_provider_client/data_provider_client
  firebase_authentication_client:
    path: packages/authentication_client/firebase_authentication_client
  firebase_data_provider_client:
    path: packages/data_provider_client/firebase_data_provider_client
  firebase_storage_client:
    path: packages/storage_client/firebase_storage_client
  models:
    path: packages/models
  tasks_repository:
    path: packages/tasks_repository
  horse_repository:
    path: packages/horse_repository
  users_repository:
    path: packages/users_repository
  barns_repository:
    path: packages/barns_repository
  invoices_repository:
    path: packages/invoices_repository
  invitations_repository:
    path: packages/invitations_repository
  ride_logs_repository:
    path: packages/ride_logs_repository
  file_picker: ^10.3.3
  firebase_crashlytics: ^5.0.1
  firebase_analytics: ^12.0.4
  flutter_stripe: ^12.1.1
  cloud_functions: ^6.0.2
  webview_flutter: ^4.13.0
  firebase_messaging: ^16.0.2
  firebase_auth: ^6.1.0
  flutter_local_notifications: ^19.4.2
  flutter_svg: ^2.2.1
  share_plus: ^12.0.1
  intl_phone_number_input: ^0.7.5

dev_dependencies:
  build_runner: ^2.4.9
  flutter_launcher_icons: ^0.14.4
  mocktail: ^1.0.4
  flutter_test:
    sdk: flutter
  freezed: ^3.1.0
  riverpod_generator: ^2.6.5
  very_good_analysis: 9.0.0

flutter:
  generate: true
  uses-material-design: true
#  assets:
#    - env-dev
#    - env-prod